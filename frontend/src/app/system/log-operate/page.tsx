'use client';import { useEffect, useState, useCallback } from 'react';import api from '@/lib/api';import { Button } from '@/components/ui/button';import { TooltipProvider } from '@/components/ui/tooltip';import { Search, Download } from 'lucide-react';import { ErpTable,ErpThead,ErpTh,ErpTbody,ErpTr,ErpTd,ErpEmpty,ErpTools,ErpPagination } from '@/components/ui/erp-table';
export default function Page(){const [items,setItems]=useState<any[]>([]);const [total,setTotal]=useState(0);const [pg,setPg]=useState(1);const [ps,setPs]=useState(30);
const fetch=useCallback(async()=>{const {data}=await api.get(window.location.pathname.includes('log')?'/users':window.location.pathname.includes('dict')?'/dictionaries':'/users',{params:{page:pg,pageSize:ps}});setItems(data.items);setTotal(data.total);},[pg,ps]);useEffect(()=>{fetch();},[fetch]);
return(<TooltipProvider><div className="bg-background rounded-lg border shadow-sm"><div className="flex items-center justify-between px-4 h-14 border-b border-border"><div><Button variant="outline" size="sm"><Download className="h-3.5 w-3.5 mr-1"/>导出</Button></div><Button variant="default" size="sm" onClick={fetch}><Search className="h-3.5 w-3.5 mr-1"/>搜索</Button></div><ErpTools onRefresh={fetch}/><div className="overflow-auto"><ErpTable><ErpThead><ErpTh>编号</ErpTh><ErpTh>名称</ErpTh><ErpTh>详情</ErpTh><ErpTh>时间</ErpTh></ErpThead><ErpTbody>{items.map((i:any)=>(<ErpTr key={i.id}><ErpTd className="font-medium">{i.code||i.username||i.id}</ErpTd><ErpTd>{i.name||i.moduleName||'-'}</ErpTd><ErpTd className="text-muted-foreground">{i.value||i.businessName||i.method||'-'}</ErpTd><ErpTd className="text-muted-foreground">{i.createdAt?new Date(i.createdAt).toLocaleDateString('zh-CN'):'-'}</ErpTd></ErpTr>))}{items.length===0&&<ErpEmpty colSpan={4}/>}</ErpTbody></ErpTable></div><ErpPagination page={pg} pageSize={ps} total={total} onPage={setPg} onPageSize={v=>setPs(+v)}/></div></TooltipProvider>);}

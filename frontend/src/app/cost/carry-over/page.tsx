'use client';import { useEffect, useState, useCallback } from 'react';import api from '@/lib/api';import { Button } from '@/components/ui/button';import { Input } from '@/components/ui/input';import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from '@/components/ui/select';import { exportCSV } from '@/lib/export';
import { TooltipProvider } from '@/components/ui/tooltip';import { Download, Search } from 'lucide-react';import { ErpTable,ErpThead,ErpTh,ErpTbody,ErpTr,ErpTd,ErpEmpty,ErpTools,ErpPagination } from '@/components/ui/erp-table';
interface Item { id:string;transactionNo:string;transactionType:string;materialName:string|null;quantity:string|null;unitPrice:string|null;totalAmount:string|null;transactionDate:string; }
export default function CostCarryOver(){const [items,setItems]=useState<Item[]>([]);const [total,setTotal]=useState(0);const [pg,setPg]=useState(1);const [ps,setPs]=useState(30);const [s,setS]=useState({type:''});
const fetch=useCallback(async()=>{const p:any={page:pg,pageSize:ps};if(s.type)p.type=s.type;const {data}=await api.get('/cost-ledgers',{params:p});setItems(data.items);setTotal(data.total);},[pg,ps,s]);useEffect(()=>{fetch();},[fetch]);
return(<TooltipProvider><div className="bg-background rounded-lg border shadow-sm"><div className="flex items-center justify-between px-4 h-14 border-b border-border"><div><Button variant="outline" size="sm" onClick={()=>exportCSV(items,'成本台账')}><Download className="h-3.5 w-3.5 mr-1"/>导出</Button></div><div className="flex items-center gap-1"><Button variant="ghost" size="sm" onClick={()=>setS({type:''})}>重置</Button><Button variant="default" size="sm" onClick={fetch}><Search className="h-3.5 w-3.5 mr-1"/>搜索</Button></div></div><div className="flex items-center gap-4 px-4 py-2.5 border-b border-border bg-muted/30"><F label="类型"><Select value={s.type} onValueChange={(v:any)=>setS({...s,type:v==='ALL'?'':v})}><SelectTrigger className="w-[110px] h-9 rounded-md border border-border bg-background px-3 text-[13px]"><SelectValue placeholder="全部"/></SelectTrigger><SelectContent><SelectItem value="ALL">全部</SelectItem><SelectItem value="入库">入库</SelectItem><SelectItem value="出库">出库</SelectItem><SelectItem value="调拨">调拨</SelectItem><SelectItem value="调整">调整</SelectItem></SelectContent></Select></F></div><ErpTools onRefresh={fetch}/><div className="overflow-auto"><ErpTable><ErpThead><ErpTh>交易单号</ErpTh><ErpTh>类型</ErpTh><ErpTh>物料</ErpTh><ErpTh>数量</ErpTh><ErpTh>单价</ErpTh><ErpTh>金额</ErpTh><ErpTh>日期</ErpTh></ErpThead><ErpTbody>{items.map(i=>(<ErpTr key={i.id}><ErpTd className="font-medium">{i.transactionNo}</ErpTd><ErpTd>{i.transactionType}</ErpTd><ErpTd className="text-muted-foreground">{i.materialName||'-'}</ErpTd><ErpTd>{i.quantity||'-'}</ErpTd><ErpTd>{i.unitPrice?Number(i.unitPrice).toFixed(2):'-'}</ErpTd><ErpTd className="font-medium">{i.totalAmount?Number(i.totalAmount).toLocaleString():'-'}</ErpTd><ErpTd className="text-muted-foreground">{i.transactionDate?new Date(i.transactionDate).toLocaleDateString('zh-CN'):'-'}</ErpTd></ErpTr>))}{items.length===0&&<ErpEmpty colSpan={7}/>}</ErpTbody></ErpTable></div><ErpPagination page={pg} pageSize={ps} total={total} onPage={setPg} onPageSize={v=>setPs(+v)}/></div></TooltipProvider>);}
function F({label,children}:{label:string;children:React.ReactNode}){return<div className="flex items-center gap-1.5"><span className="text-[13px] text-muted-foreground w-[80px] text-right shrink-0">{label}</span>{children}</div>;}

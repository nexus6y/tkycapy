'use client';import { useEffect, useState, useCallback } from 'react';import { useRouter } from 'next/navigation';import api from '@/lib/api';import { Button } from '@/components/ui/button';import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from '@/components/ui/select';import { Checkbox } from '@/components/ui/checkbox';import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle } from '@/components/ui/alert-dialog';import { TooltipProvider } from '@/components/ui/tooltip';import { Download, Pencil, Plus, Search, Trash2 } from 'lucide-react';import { ErpTable,ErpThead,ErpTh,ErpTbody,ErpTr,ErpTd,ErpEmpty,ErpLink,ErpAction,ErpActionBtn,ErpTools,ErpApproval,ErpPagination } from '@/components/ui/erp-table';
interface Item { id:string;orderNo:string;type:string;materialName:string|null;quantity:string|null;borrower:string|null;borrowDate:string;expectedReturn:string|null;actualReturn:string|null;approvalStatus:string; }
export default function LendOrder(){const router=useRouter();const [items,setItems]=useState<Item[]>([]);const [total,setTotal]=useState(0);const [pg,setPg]=useState(1);const [ps,setPs]=useState(30);const [del,setDel]=useState<string|null>(null);
const fetch=useCallback(async()=>{const {data}=await api.get('/lend-orders',{params:{page:pg,pageSize:ps}});setItems(data.items);setTotal(data.total);},[pg,ps]);useEffect(()=>{fetch();},[fetch]);
const doDel=async()=>{if(!del)return;await api.delete(`/lend-orders/${del}`);setDel(null);fetch();};
return(<TooltipProvider><div className="bg-background rounded-lg border shadow-sm"><div className="flex items-center justify-between px-4 h-14 border-b border-border"><div className="flex items-center gap-1"><Button variant="secondary" size="sm" onClick={()=>router.push('/warehouse/lend-order/create')}><Plus className="h-3.5 w-3.5"/>新增</Button><Button variant="outline" size="sm">修改</Button><Button variant="outline" size="sm">删除</Button><Button variant="outline" size="sm"><Download className="h-3.5 w-3.5 mr-1"/>导出</Button></div><Button variant="default" size="sm" onClick={fetch}><Search className="h-3.5 w-3.5 mr-1"/>搜索</Button></div><ErpTools onRefresh={fetch}/><div className="overflow-auto"><ErpTable><ErpThead><ErpTh className="w-10"><Checkbox/></ErpTh><ErpTh>审批状态</ErpTh><ErpTh>借出单号</ErpTh><ErpTh>物料</ErpTh><ErpTh>数量</ErpTh><ErpTh>借用人</ErpTh><ErpTh>借出日期</ErpTh><ErpTh>预计归还</ErpTh><ErpTh>实际归还</ErpTh><ErpTh>操作</ErpTh></ErpThead><ErpTbody>{items.map(i=>(<ErpTr key={i.id}><ErpTd><Checkbox/></ErpTd><ErpTd><ErpApproval status={i.approvalStatus}/></ErpTd><ErpTd><ErpLink>{i.orderNo}</ErpLink></ErpTd><ErpTd className="text-muted-foreground">{i.materialName||'-'}</ErpTd><ErpTd>{i.quantity||'-'}</ErpTd><ErpTd>{i.borrower||'-'}</ErpTd><ErpTd className="text-muted-foreground">{i.borrowDate?new Date(i.borrowDate).toLocaleDateString('zh-CN'):'-'}</ErpTd><ErpTd className="text-muted-foreground">{i.expectedReturn?new Date(i.expectedReturn).toLocaleDateString('zh-CN'):'-'}</ErpTd><ErpTd className="text-muted-foreground">{i.actualReturn?new Date(i.actualReturn).toLocaleDateString('zh-CN'):'-'}</ErpTd><ErpTd><ErpAction>{i.approvalStatus==='DRAFT'&&<ErpActionBtn onClick={async()=>{await api.put('/lend-orders/'+i.id+'/submit');fetch();}}>提交</ErpActionBtn>}{i.approvalStatus==='SUBMITTED'&&<ErpActionBtn onClick={async()=>{await api.put('/lend-orders/'+i.id+'/approve');fetch();}}>通过</ErpActionBtn>}<ErpActionBtn onClick={()=>router.push(`/warehouse/lend-order/${i.id}/edit`)}><Pencil className="h-3.5 w-3.5"/>修改</ErpActionBtn><ErpActionBtn danger onClick={()=>setDel(i.id)}><Trash2 className="h-3.5 w-3.5"/>删除</ErpActionBtn></ErpAction></ErpTd></ErpTr>))}{items.length===0&&<ErpEmpty colSpan={10}/>}</ErpTbody></ErpTable></div><ErpPagination page={pg} pageSize={ps} total={total} onPage={setPg} onPageSize={v=>setPs(+v)}/><AlertDialog open={!!del} onOpenChange={()=>setDel(null)}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>确认删除？</AlertDialogTitle></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>取消</AlertDialogCancel><AlertDialogAction onClick={doDel} className="bg-[#f56c6c] text-white hover:bg-[#f56c6c]/90">删除</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog></div></TooltipProvider>);}

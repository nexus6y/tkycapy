'use client';import { useState } from 'react';import { Button } from '@/components/ui/button';import { Input } from '@/components/ui/input';import { TooltipProvider } from '@/components/ui/tooltip';import { Download, Plus, Search } from 'lucide-react';import { ErpTable,ErpThead,ErpTh,ErpTbody,ErpEmpty,ErpTools,ErpPagination } from '@/components/ui/erp-table';
function tip(){alert('功能开发中，敬请期待');}
export default function Page(){return(<TooltipProvider><div className="bg-background rounded-lg border shadow-sm"><div className="flex items-center justify-between px-4 h-14 border-b border-border"><div className="flex items-center gap-1"><Button variant="secondary" size="sm" onClick={tip}><Plus className="h-3.5 w-3.5"/>新增</Button><Button variant="outline" size="sm" onClick={tip}>修改</Button><Button variant="outline" size="sm" onClick={tip}>删除</Button><Button variant="outline" size="sm" onClick={tip}><Download className="h-3.5 w-3.5 mr-1"/>导出</Button></div><div className="flex items-center gap-1"><Button variant="ghost" size="sm" onClick={tip}>重置</Button><Button variant="default" size="sm" onClick={tip}><Search className="h-3.5 w-3.5 mr-1"/>搜索</Button></div></div><ErpTools onRefresh={()=>alert('刷新')}/><div className="overflow-auto"><ErpTable><ErpThead><ErpTh>编码</ErpTh><ErpTh>名称</ErpTh><ErpTh>状态</ErpTh><ErpTh>创建时间</ErpTh><ErpTh>操作</ErpTh></ErpThead><ErpTbody><ErpEmpty colSpan={5}/></ErpTbody></ErpTable></div><ErpPagination page={1} pageSize={30} total={0} onPage={()=>tip()} onPageSize={()=>tip()}/></div></TooltipProvider>);}
